fastapi==0.115.6
uvicorn==0.34.0

# Added in later phases (kept here so the file stays the single source of truth):
# pymupdf     — Phase 1, PDF text-layer extraction
# jieba       — Phase 2, segmentation/POS tagging
# genanki     — Phase 4, .apkg export
# spacy       — Phase 7, POS accuracy pass
# pkuseg      — Phase 7, POS accuracy pass

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the detachable chat/dictionary popouts (Phase 6)",
  "windows": ["main", "chat", "dictionary"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "core:webview:allow-create-webview-window"
  ]
}
